class Expense {
  constructor(id, name, amount, date, optional) {
    this.id = id;
    this.name = name;
    this.amount = amount;
    this.date = date;
    this.optional = optional;
  }
}

export default Expense;
